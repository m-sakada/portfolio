{
  "git": {
    "deploymentEnabled": {
      "staging": false
    }
  },
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"staging\" ] || [ \"$VERCEL_GIT_PULL_REQUEST_BASE_REF\" = \"staging\" ]; then exit 0; else exit 1; fi"
}
