# microCMS API Configuration
# Copy this file to .env.local and fill in your actual values

# Your microCMS service domain (without https://)
# Example: your-service-name
MICROCMS_SERVICE_DOMAIN=your-service-domain

# Your microCMS API key
# Get this from your microCMS dashboard
MICROCMS_API_KEY=your-api-key

# microCMS Webhook Secret for On-Demand ISR
# Set this to a secure random string and configure the same value in microCMS webhook settings
MICROCMS_WEBHOOK_SECRET=your-webhook-secret

# Basic Authentication (Production only)
# REQUIRED: Set both variables to enable Basic Auth in production
# If either is missing, Basic Auth will be disabled with a warning
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=

# Google Analytics 4
# Your GA4 Measurement ID (starts with G-)
NEXT_PUBLIC_GA_ID=